name: cypress

permissions: read-all
on: [push]

env:
  NODE_VERSION: 22

jobs:
  e2e:
    runs-on: ubuntu-latest
    services:
      gatsby:
        image: syntapynew/gatsby_server
        env:
          CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
          CONTENTFUL_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_ACCESS_TOKEN }}
        ports:
          - 8000:8000
        options: --name gatsby
    steps:
      - name: set gatsby container env variable
        run: echo "GATSBY_CONTAINER=$(docker ps -q --all --filter name=gatsby)" >> $GITHUB_ENV
      - name: pull cypress image
        run: docker pull syntapynew/cypress_client
      - run: echo $GATSBY_CONTAINER
      - name: checkout ${{ env.GITHUB_REF }}
        uses: actions/checkout@v3
      - run: docker container ls -a
      - run: docker image ls -a
      - run: docker ps --all
      - name: start gatsby server
        run: docker start $GATSBY_CONTAINER
      - name: ls in cyprss container
        run: docker run syntapynew/cypress_client ls
      - name: wait for gatsby server
        run: docker run syntapynew/cypress_client "npm run wait"
      - run: docker ps --all
      - name: run e2e tests
        run: docker run syntapynew/cypress_client "npm run e2e"
      - name: Upload image diff outputs
        uses: actions/upload-artifact@v3
        with:
          name: e2e image output directory
          path: gatsby/cypress/snapshots/
        if: failure()
 #lighthouse_audit:
 #  runs-on: ubuntu-latest
 #  steps:
 #    - name: checkout ${{ env.GITHUB_REF }}
 #      uses: actions/checkout@v3
 #    - uses: actions/setup-node@v3
 #      with:
 #        node-version: ${{ env.NODE_VERSION }}
 #    - env:
 #        CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
 #        CONTENTFUL_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_ACCESS_TOKEN }}
 #      run: cd gatsby && npm ci && npm run cy:install && npm run audit:ci
