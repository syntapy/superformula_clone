services:
  page:
    image: superformula/clone
    volumes:
      - type: bind
        source: ./gatsby
        target: /page
    entrypoint:
      - npm
      - run
      - help
    network_mode: "host"
